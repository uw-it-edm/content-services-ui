/**
 * Configuration settings for multi-select fields.
 */
export interface MultiSelectConfig {
  /**
   * The maximum number of selections allowed, infinite if not defined.
   */
  maximumSelectionCount?: number;
}
