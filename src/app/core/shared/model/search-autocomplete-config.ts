export class SearchAutocompleteConfig {
  type: string;
  filterKey: string;
  filterLabel: string;

  /**
   * Optional settings used by 'SearchFilterableResult' to format its display value.
   */
  optionDisplayConfig?: any;
}
