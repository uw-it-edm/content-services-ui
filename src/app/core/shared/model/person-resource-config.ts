/**
 * Configuration settings for fields that load person resources.
 */
export interface PersonResourceConfig {
  /**
   * Whether to display the person's registered name instead of the default preferred name.
   */
  displayRegisteredName?: boolean;
}
